지금까지 몽고db 의 핵심인 nesting 부분을 다뤘습니다.
nesting 에서의 핵심은 데이터베이스 호출을 줄여야 한다는 것입니다.
1차적으로 POPULATE 로 줄이고,
더 나아가서 nesting 으로 줄여주었습니다.
호출 빈도를 낮추는 것에 집중했습니다.
스키마 (데이터 구조) 설계 차원에서 중요한 부분입니다.
하지만 api 에서의 operation 이 늘어났습니다. 탐색(find) 를 무조건 먼저 했습니다.

이번에는 탐색 자체를 빠르게 하는 것에 대해 다룰 것 입니다. 데이터가 많아지면 탐색이 느려집니다. 그것을 해결하는 것이 INDEX 의 기능입니다.